spring:
  data:
    redis:
      host: localhost
      port: 6379

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${RESERVAE_KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/reservae}

feign:
  client:
    config:
      event-catalog-service:
        connectTimeout: 5000
        readTimeout: 5000