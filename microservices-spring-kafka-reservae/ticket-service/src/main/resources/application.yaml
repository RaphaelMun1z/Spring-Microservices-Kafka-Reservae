server:
  port: 8500

spring:
  application:
    name: ticket-service
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: dev

reservae:
  config:
    kafka:
      server-url: localhost:29092
      consumer-groups:
        pedido-confirmado: ticket-service-order-confirmed
      topics:
        pedido-confirmado: reservae.order.confirmed.v1
        ingresso-gerado: reservae.ticket.generated.v1
  security:
    required-audience: reservae-api

springdoc:
  api-docs:
    path: /${spring.application.name}/v3/api-docs